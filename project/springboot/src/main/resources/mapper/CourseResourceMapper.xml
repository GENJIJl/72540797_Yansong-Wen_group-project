<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springboot.dao.CourseResourceDao">


    <select id="findBySearch" resultType="com.example.springboot.entity.CourseResource">
        select `courseResource`.* from `courseResource`

        where 1 = 1
        <if test="search != null and search.title != null and search.title != ''">
            and `courseResource`.`title` like concat ('%', #{search.title}, '%')
        </if>

    </select>

</mapper>